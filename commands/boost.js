const Discord = require("discord.js");
const db = require("../db")
const os = require('os')
module.exports = {
    name: 'boost',
    description: 'cmd to buy boosts',
    cooldown: 5,
    async execute(message, args, client) {
      return;  
    },
};

